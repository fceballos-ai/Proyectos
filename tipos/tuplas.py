# Tuplas (no se pueden modificar)

coordenadas = (10, 20)
print(coordenadas)
print(coordenadas[1])
# Las tuplas son inmutables, por lo que no se pueden agregar ni eliminar elementos
# coordenadas.append(30)  # Esto generaría un error
# coordenadas.remove(10)  # Esto también generaría un error
# Sin embargo, se pueden desempaquetar las tuplas 